# aluppol.github.io

User site for **albert.luppol.com**. Serves the landing page; every project Pages site of this account is served under the same domain automatically (`albert.luppol.com/<repo>`), per GitHub Pages custom-domain inheritance.

- `CNAME` — the custom domain.
- `index.html` — placeholder landing page, to be replaced by the projects hub.
